#include "nucleus_decomposition.h"

void run_nucleus_decomposition(Graph* graph, int r, int s) {
    printf("Running Nucleus Decomposition with r = %d and s = %d.\n", r, s);
    graph_print(graph, true);
}
